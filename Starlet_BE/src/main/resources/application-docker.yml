spring:
  datasource:
    url: jdbc:mysql://${db.hostname}:${db.port}/${db.name}
    username: ${db.username}
    password: ${db.password}

  sql:
    init:
      mode: always
      platform: mysql
      encoding: utf-8